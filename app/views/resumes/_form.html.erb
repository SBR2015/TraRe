<div class="form-resume">
<%= form_for([@user, @resume]) do |f| %>
  <% if @resume.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resume.errors.count, "error") %> prohibited this resume from being saved:</h2>

      <ul>
      <% @resume.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= f.label :sentence %><br>
    <%= f.text_area :sentence, class: 'form-control', rows: 10, placeholder: 'your story' %>
  </div>
  <!--<div class="field">-->
  <!--<%= f.label :is_translation %><br>-->
    <!--<%= f.check_box :is_translation %>-->
  <!--</div>-->
  <!--<div class="field">-->
  <!--<%= f.label :owner_id %><br>-->
    <!--&lt;!&ndash;<%= f.number_field :owner_id %>&ndash;&gt;-->
    <!--&lt;!&ndash;<%= f.select :owner_id, User.all.map { |u| [u.username, u.id] } %>&ndash;&gt;-->
    <!--<%= f.select :owner_id, {@user.username => @user.id} %>-->
  <!--</div>-->
  <!--<div class="field">-->
  <!--<%= f.label :translator_id %><br>-->
    <!--&lt;!&ndash;<%= f.number_field :translator_id %>&ndash;&gt;-->
    <!--&lt;!&ndash;<%= f.select :translator_id, User.all.map { |u| [u.username, u.id] } %>&ndash;&gt;-->
    <!--<%= f.select :translator_id, {current_user.username => current_user.id} %>-->

  <!--</div>-->
  <div class="form-group">
    <%= f.label :language_id %><br>
    <%#= f.select :language_id, Language.all.map { |l| [l.code, l.id] } %>
    <%= f.select :language_id, @language, {}, class: 'form-control' %>

  </div>
  <div class="actions">
    <% if current_page?(action: 'new') %>
      <%= f.submit 'create', class: 'btn btn-primary' %>
    <% else %>
      <%= f.submit 'update', class: 'btn btn-primary' %>
    <% end %>
  </div>
<% end %>

<%= link_to 'Back', user_resumes_path %>
</div>
